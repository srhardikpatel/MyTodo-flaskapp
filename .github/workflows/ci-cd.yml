name: CI-CD Pipeline

on:
  push:
    branches: [main]

jobs:

  build:
    name: Build image and upload
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: docker build -t erhardikfreelancer/flaskapp:${BUILD_NUMBER} .
